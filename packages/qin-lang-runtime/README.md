# qin-lang-runtime

Runtime library for Qin language programs.

## Scope

- Built-in APIs like `console`, `fs`, and `java-interop`
- Runtime value model and host bindings
- Shared by interpreter and compiled outputs

